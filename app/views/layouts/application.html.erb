<!DOCTYPE html>
<html>
  <head>
    <title>DudMain</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= yield :custom_head %>
  </head>

  <body>
  <%= render 'layouts/menu' %>
  <div class="container">
    <%= yield %>
  </div>
    <%= yield :scripts %>
  </body>
</html>
