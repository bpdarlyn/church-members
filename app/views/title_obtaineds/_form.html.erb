<%= form_with(model: title_obtained, local: true) do |form| %>
  <% if title_obtained.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(title_obtained.errors.count, "error") %> prohibited this title_obtained from being saved:</h2>

      <ul>
        <% title_obtained.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title_type %>
    <%= form.text_field :title_type %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
