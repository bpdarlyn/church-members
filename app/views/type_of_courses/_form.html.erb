<%= form_with(model: type_of_course, local: true) do |form| %>
  <% if type_of_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_of_course.errors.count, "error") %> prohibited this type_of_course from being saved:</h2>

      <ul>
        <% type_of_course.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :code %>
    <%= form.number_field :code %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
